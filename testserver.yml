ratelimiter:
  global:
    limit: 200
    duration: 1m
    keyType: USER

  apis:
    global-api:
      strategy: TOKEN_BUCKET
      keyType: USER
      limit: 5
      duration: 10
    test-api:
      strategy: SLIDING_WINDOW
      keyType: USER
      limit: 4
      burstlimit: 3
      duration: 10
