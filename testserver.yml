ratelimiter:
  global:
    limit: 200
    duration: 1
    keyType: USER

  apis:
    test-api:
      keyType: USER
      limit: 5
      duration: 11
management:
  endpoints:
    web:
      exposure:
        include: "*"
